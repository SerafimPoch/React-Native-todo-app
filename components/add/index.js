import React, { Fragment, Component } from "react";
import { Text } from "react-native";

class Add extends Component {
  render() {
    return (
      <Fragment>
        <Text>Add</Text>
      </Fragment>
    );
  }
}

export default Add;
